Run the Go build check and tests for this Wordle project.

Execute the following commands sequentially and report their output:

1. `go build ./...` — verifies the package compiles cleanly
2. `go test ./...` — runs all tests (reports "no test files" if none exist yet)

Print any errors clearly and suggest fixes if the build or tests fail.
